// Higher Order Function created to wrap multiple elements 
// replaces need for using a wrapping div to negate inability to return
// multiple elements next to one another

const aux = (props) => props.children;

export default aux;